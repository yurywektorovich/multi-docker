sudo: required
services:
  - docker

before_install:
  - docker build -t yurywektorovich/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run yurywektorovich/react-test npm test -- --coverage

after_success:
  - docker build -t yurywektorovich/multi-client ./client
  - docker build -t yurywektorovich/multi-nginx ./nginx
  - docker build -t yurywektorovich/multi-server ./server
  - docker build -t yurywektorovich/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push yurywektorovich/multi-client
  - docker push yurywektorovich/multi-nginx
  - docker push yurywektorovich/multi-server
  - docker push yurywektorovich/multi-worker
